type Todo {
  id: Int!
  text: String!
  completed: Boolean!
}

type Mutation {
  addTodo(text: String!): Todo
  toggleTodo(id: Int!): Todo
}

extend type Query {
  visibilityFilter: String
  todos: [Todo]
}
